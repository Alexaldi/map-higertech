# 1. HTTP Redirect to HTTPS
server {
    listen 80;
    server_name core8.higertech.com localhost;

    location / {
        return 301 https://$host$request_uri;
    }
}

# 2. HTTPS Server
server {
    listen 443 ssl;
    http2 on;
    server_name core8.higertech.com localhost;

    root /var/www/html/public;
    index index.php;

    # SSL Certificates
    ssl_certificate /etc/letsencrypt/live/core8.higertech.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/core8.higertech.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Max upload size (sesuai php.ini)
    client_max_body_size 20M;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
    gzip_min_length 256;

    # Laravel routing — semua ke index.php kecuali file statis
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM handler
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }

    # Cache static assets
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot|webp)$ {
        expires 30d;
        add_header Cache-Control "public, no-transform";
        try_files $uri =404;
    }

    # Deny .htaccess access
    location ~ /\.ht {
        deny all;
    }

    # Deny hidden files
    location ~ /\. {
        deny all;
    }
}

